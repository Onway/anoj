1000: 
信号处理方式测试。
在配置文件指定SIGUSR1信号的三个行为，被测程序向自身发送信号。
输入输出为单例一般测试。
由于SIGUSR1默认为终止程序，
当指定为0的时候，即拦截后不向程序传递，则可见AC;
当指定为1的时候，即拦截后向程序传递，则程序会被信号终止，在err处可见信息
当指定万2的时候，即拦截后直接终止程序，在msg处可见信息。

1001:
实时信号测试。
同1000原理。

1002:
系统调用测试
使用SYS_getuid32, SYS_getgid32

1003:
删除环境变量DELENV

1004:
添加环境变量OJENV

1005:
资源限制设置测试
添加资源限制RLIMIT_NOFILE，读取然后输出

1006:
python解析器内存使用测试
空载运行。

1007:
超时测试
使用睡眠时间

1008:
内存限制测试
测试数据段内存，栈内存

Main:
java a + b
