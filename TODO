judger:
* 1，php完全没测试。第一版不支持。
* 5，pascal未开始。第一版不支持。
* 11，c程序定义全局变量不会超内存。(virtual memory, it doesn't matter)
12，用户程序在/tmp目录写文件
* 14，java输出过滤
* 16，除SIGXCPU外，用户进程的任何退出原因都应该先判断是否超时。
18，评测任务的并发问题没有解决。
* 19，output limit for java takes no effect. 使用了另一种方式解决。
* 20，SIGXCPU terminate process, but not stop by ptrace for java. 使用另一种方式解决了。
21，timeout has a few deviation.
* 22，what if program use raise(SIGTRAP)? kill syscall is not availible!
23，cgi子进程未释放网络连接。
24，java的fork炸弹没效果？不应该啊！
25，安装后，spj工作异常

